#include<stdio.h>
#include "math_utils.h"
#include<stdlib.h>

int main(){
    int x = 10, y = 3;
    printf("Add: %d\n", add(x, y));
    printf("Subtract: %d\n", subtract(x, y));
    printf("Square: %d\n", square(5));
    //getchar();
    system("pause");
    return 0;
}

